/**
 * Save component for the video block.
 *
 * Returns null because rendering is handled server-side via PHP
 * render_callback in VideoBlock.php.
 *
 * @returns {null} Always null (dynamic block).
 */
export default function save() {
    return null;
}
